# Runs (local)

This folder stores **local** run artifacts produced by `train.py`:
- `config.json`, `meta.json`, `metrics.json`
- `model.joblib`
- `test_outputs.npz` (y_true/y_pred)

By default, `runs/**` is **gitignored** (see `.gitignore`) to keep the repository lightweight.

